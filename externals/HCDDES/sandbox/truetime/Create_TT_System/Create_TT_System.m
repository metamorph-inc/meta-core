function Create_TT_System(env, schedule, additional_rules)

% schedule would be imported from joe's stuff and the additional rules would 
% have to be generated by hand dependent on the model and would be associated
% with the additional_blocks.mdl file.

Create_Generic_Code();
Create_Specific_Simulink(schedule, additional_rules, env);
